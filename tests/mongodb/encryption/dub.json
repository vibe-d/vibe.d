{
    "name": "tests",
    "description": "CSFLE ClientEncryption + MongoCryptProvider dependency-injection seam integration test for vibe.d (no live server required)",
    "dependencies": {
        "vibe-d:mongodb": {"path": "../../../"},
        "openssl": "~>3.4"
    }
}
